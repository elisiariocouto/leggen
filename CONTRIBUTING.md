# Contributing

This project uses **uv** for Python dependency management and **shadcn/ui** for frontend components.

## Setup
Install uv and run `uv sync` to install dependencies.

Run `pre-commit install` to install the pre-commit hooks.

## Frontend Development
The frontend uses shadcn/ui components for consistent design. When adding new UI components:
- Check if a shadcn/ui component exists for your use case
- Follow the existing component patterns in `frontend/src/components/ui/`
- Use Tailwind CSS classes for styling
- Ensure components are accessible and follow the design system

## Commit messages

type(scope/[subscope]): Title starting with uppercase and sentence ending with period.
More than 80 charactes use the body of the commit message.

Scope and subscopes are optional.

DO NOT use a bunch of different types: feat, fix, refactor should be more than enough.

### Breaking changes

The changelog is generated by git-cliff, and it renders only the subject line of
each commit. Anything a user has to *act on* — a new minimum supported version, a
config key that was removed, a manual upgrade step — is invisible unless the
commit is marked as breaking.

Mark it two ways: a `!` after the scope, and a `BREAKING CHANGE:` footer holding
the instructions. Keep the footer on one line, since it is rendered verbatim into
the release notes.

```
refactor(api)!: Replace ad-hoc migrations with a versioned migration runner.

<body>

BREAKING CHANGE: Databases older than release 2025.9.22 are no longer upgraded and leggen refuses to start on one. Upgrade to a release between 2025.9.22 and 2026.8.x, start leggen once so it migrates, then upgrade to this version.
```

Preview what a release will say with `git-cliff --unreleased`.

## Release new version

Run `scripts/release.sh`. It takes no arguments.

Versions are CalVer, `YEAR.MONTH.MICRO` — the year and month come from today's
date and the micro auto-increments from the last tag in the same month, so the
first release of a month is `.0`. The script bumps `pyproject.toml` and
`frontend/package.json`, regenerates `CHANGELOG.md` with git-cliff, locks
dependencies, commits, and tags; it then asks before pushing. Pushing the tag is
what triggers the PyPI and Docker publishing workflows.

Requires `git-cliff`, `uv`, and `npm` on PATH.
